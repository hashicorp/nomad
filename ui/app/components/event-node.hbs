{{!-- <rect
  class="bar {{@data.name}}"
  x={{this.x}}
  y={{this.y}}
  width={{this.width}}
  height={{this.height}}
></rect> --}}

<circle
  class="event-node {{if @data.highlight "highlight"}}  {{if @data.blur "blur"}}"
  cx={{this.x}}
  cy={{this.y}}
  r={{this.r}}
  data-type={{@data.Type}}
  data-topic={{@data.Topic}}
  {{action @logEvent @data}}
  {{on "mouseenter" this.onMouseEnter}}
  {{on "mouseleave" this.onMouseLeave}}
 />

 {{!-- {{log "NODE" @data.x}} --}}