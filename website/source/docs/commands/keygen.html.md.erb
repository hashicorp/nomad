---
layout: "docs"
page_title: "Commands: Keygen"
sidebar_current: "docs-commands-keygen"
description: >
  The `keygen` command generates an encryption key that can be used for Nomad
  agent traffic encryption. The keygen command uses a cryptographically strong
  pseudo-random number generator to generate the key.

---

# Command: keygen

The `keygen` command generates an encryption key that can be used for Nomad agent traffic encryption.
The keygen command uses a cryptographically
strong pseudo-random number generator to generate the key.

## Examples

Generate an encryption key:

```
$ nomad keygen
lUTicIcJiJ0SlEGStS1eMA==
```
