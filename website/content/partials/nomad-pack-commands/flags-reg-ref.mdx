- `-registry=<string>` - Name of the specific registry containing the targeted
  pack.

- `-ref=<string>` - Specific git ref of the registry or pack to be targeted.
  Supports tags, SHA, and latest. If no `ref` is specified, defaults to latest.

  <Note>

  Using `ref` with a file path is not supported.

  </Note>
