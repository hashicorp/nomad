---
layout: docs
page_title: 'Commands: event sink deregister'
sidebar_title: sink deregister
description: |
  The event command is used to deregistered an event sink.
---

# Command: event sink deregister

The `event sink deregister` command is used to deregister an event sink. Deregistering an event sink will remove the event sink from Nomad, stopping it from sending events to the sink.

## Usage

```plaintext
nomad event sink deregister <event sink id>
```

The `event sink deregister`

## General Options

@include 'general_options.mdx'

## Example

Deregister an event sink:

```shell-session
$ nomad event sink deregister job-webhook
Successfully deregistered "job-webhook" event sink!
```

