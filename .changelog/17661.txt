```release-note:improvement
jobspec: Add new parameter `render_template_on_restart` for `task` to allow explicit re-render of templates on task restart. The default value is `false` and is fully backward compatible
```
