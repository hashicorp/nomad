---
layout: commands
page_title: "Commands: Generate pack"
sidebar_title: "generate pack"
description: "Generate a new pack."
---

# Nomad-Pack Generate pack

Command: `nomad-pack generate pack`

Generate a new pack.

@include "tools/nomad-pack/commands/generate-pack_desc.mdx"

## Usage

Usage: `nomad-pack generate pack <name>`


	Generate a new pack.

Examples:
	# Create a new pack named "my-new-pack" in the current directory.
	nomad-pack generate pack my-new-pack


#### Approval Options

- `-auto-approve` - Automatically answer confirmation prompts in the
						affirmative.

#### Output Options

- `-to-dir=<string>` - Path to write generated pack to.
- `-overwrite` - If the output directory is not empty, should we overwrite?

@include "tools/nomad-pack/commands/generate-pack_more.mdx"
