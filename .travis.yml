sudo: required

language: go

go:
  - 1.5.2
  - tip

matrix:
  allow_failures:
    - go: tip

branches:
  only:
    - master

install:
  - ./scripts/install-rkt.sh
  - make bootstrap

script:
  - sudo make test
