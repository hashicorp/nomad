# Netlify is installed under the hc-nomad bot account on GitHub.

# FIXME this is overridden to deploy website/ instead of ui/

[build]
  command = "bundle exec middleman build"
  base = "website"
  publish = "website/build"
