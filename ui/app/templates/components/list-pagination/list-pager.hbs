{{#if this.visible}}
  <LinkTo
    @query={{hash currentPage=this.page}}
    class={{this.class}}
    data-test-pager={{this.test}}
    aria-label={{this.label}}
  >
    {{yield}}
  </LinkTo>
{{/if}}
