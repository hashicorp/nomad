package main

func (v *v1api) getSystemPaths() []*Path {
	// tags := []string{"System"}

	return []*Path{
		//s.mux.HandleFunc("/v1/system/gc", s.wrap(s.GarbageCollectRequest))
		//s.mux.HandleFunc("/v1/system/reconcile/summaries", s.wrap(s.ReconcileJobSummaries))
	}
}
