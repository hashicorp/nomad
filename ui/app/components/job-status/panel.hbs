{{#if this.isActivelyDeploying}}
  <JobStatus::Panel::Deploying @job={{@job}} />
{{else}}
  <JobStatus::Panel::Steady @job={{@job}} @statusMode={{@statusMode}} @setStatusMode={{@setStatusMode}} />
{{/if}}
